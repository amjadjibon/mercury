[package]
name = "mercury-benches"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bench]]
name = "event_bus"
harness = false

[[bench]]
name = "orderbook"
harness = false

[dependencies]
mercury-core = { path = "../core" }

[dev-dependencies]
criterion.workspace = true
rust_decimal.workspace = true
rust_decimal_macros.workspace = true
